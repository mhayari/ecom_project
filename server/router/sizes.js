// const { createSize, updateSize, deleteSize, allSizes, showSize, getSizeById } = require('../controllers/sizeController')
// const { isAuth, requireSignIn, isAdmin } = require('../middlewares/auth')
// const { getUserById } = require('../middlewares/users')

// const router=require('express').Router()


// router.post('/create/:userId',[requireSignIn,isAuth,isAdmin],createSize)

// router.put('/:sizeId/:userId',[requireSignIn,isAuth,isAdmin],updateSize)

// router.delete('/:sizeId/:userId',[requireSignIn,isAuth,isAdmin],deleteSize)

// router.get('/',allSizes)

// router.get('/:sizeId',showSize)

// router.param('userId',getUserById)

// router.param('sizeId',getSizeById)

// module.exports=router